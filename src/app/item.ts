export interface Item {
    title: string,
    email: string,
    context?: string,
    description?: string,
    isActive?: Boolean,
    createdAt?: string,
    updatedAt?: string,
    _id?: string,
    __v?: number
}
